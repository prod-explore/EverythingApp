/**
 * The selectable Anthropic models — shared by the global default-model
 * picker (SettingsModal.tsx) and the per-conversation switch (Header.tsx).
 * Keep in sync with cli/src/config.ts if that ever validates model names.
 */
export const MODELS = ['claude-sonnet-4-6', 'claude-opus-5', 'claude-haiku-4-5-20251001'];
